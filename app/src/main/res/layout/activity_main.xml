<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/light_cream"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/fragmentContainerFCV"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:navGraph="@navigation/main_navigation"
        app:defaultNavHost="true"
        android:name="androidx.navigation.fragment.NavHostFragment"/>


    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:layout_width="match_parent"
        android:id="@+id/bottomNavigationBarBNV"
        android:layout_height="?attr/actionBarSize"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:menu="@menu/bottom_navigation_menu"
        app:labelVisibilityMode="unlabeled"
        app:itemIconSize="20dp"
        app:itemActiveIndicatorStyle="@style/Theme.BottomNavigationView.ActiveIndicator"
        app:itemIconTint="@drawable/selector_bottom_navigation_menus"
        android:background="@drawable/background_home_bottom_navigation_bar"/>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="Button with a plus icon inside a location to create a new art"
        android:backgroundTint="@color/yellow"
        app:maxImageSize="32dp"
        android:id="@+id/createNewArtFAB"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="@id/bottomNavigationBarBNV"
        app:layout_constraintTop_toTopOf="@id/bottomNavigationBarBNV"
        app:fabSize="normal"
        app:shapeAppearanceOverlay="@style/Theme.GenerateMapArts.FloatingActionButton"
        android:src="@drawable/ic_new_map_art"/>

</androidx.constraintlayout.widget.ConstraintLayout>